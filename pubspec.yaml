name: routing_and_menu
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.5.0-177.0.dev <4.0.0'

dependencies:
  animated_tree_view: ^2.3.0
  flutter:
    sdk: flutter
  go_router: ^14.2.7
  provider: ^6.1.2
  reactive_forms: ^17.0.1

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
